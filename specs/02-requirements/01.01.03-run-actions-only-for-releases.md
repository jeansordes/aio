# Run Actions Only For Releases

Normal branch commits should not run GitHub Actions.

Release automation runs from pushed `vX.Y.Z` tags.

`CHANGELOG.md` is prepared locally during release prep and committed with the
release commit.
