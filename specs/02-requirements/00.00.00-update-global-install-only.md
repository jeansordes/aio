# Update Install Scope

Run self-update only when app runs from global install.

Do not self-update when run by:
- `npx`
- `bunx`
- local install in project

Reason:
those modes are not stable app install target.
