# Commit Roadmap Units With Conventional Commits

One completed requirement row becomes one conventional commit.

Do not mix unrelated requirement rows in one commit.

If parent rows also become `done` from that same work:
include them in the same commit.
