# Roles Bind Provider Context And Instructions

Roles define agent behavior.

A role can define:
- provider
- model
- context includes
- instructions

Project files are passed as context.

Roles must not depend on another agent's transcript. If an agent needs durable
state for later workflow steps, it writes that state into project files.
