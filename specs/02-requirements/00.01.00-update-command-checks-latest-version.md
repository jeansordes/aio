# Update Command Checks Latest Version

`aio update` checks the latest published package version.

If the latest version is not newer than the current CLI version, it reports that
the package is already up to date and does not run an installer.

If the latest version cannot be checked, the command fails clearly instead of
pretending the package is current.
