# Migrate Customizations Via Ai Agent

The upgrade flow compares the new `.aio/` to the backup and uses an AI agent to port customizations (edited providers, roles, workflows, config) from the old tree into the new one. Before migration runs, the user is prompted to choose which provider runs that migration pass.

The migration must complete successfully before the backup is removed.
