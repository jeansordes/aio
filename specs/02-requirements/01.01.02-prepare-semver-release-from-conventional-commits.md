# Prepare Semver Release From Conventional Commits

Release prep reads conventional commits since latest tag.

Rules:
- `fix` => patch
- `feat` => minor
- breaking change => major
- no qualifying commit => no release

Local prep updates version and changelog,
creates release commit,
then creates matching `vX.Y.Z` tag.
