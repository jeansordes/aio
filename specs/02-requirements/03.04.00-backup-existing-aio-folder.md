# Backup Existing Aio Folder

`aio upgrade` (or equivalent) renames the current `.aio` directory to a known backup name (for example `.aio.bak.<timestamp>`) before any re-init, so a failed run can be recovered manually from the backup.
