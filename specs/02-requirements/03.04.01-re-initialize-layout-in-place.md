# Re-Initialize Layout In Place

After backup, the command runs the same init logic as a fresh `aio init` to create a new `.aio/` with the current template. Existing files in the new tree follow init rules: missing files are created; the command must not delete user files inside the new `.aio` that init does not own.

This requirement depends on a successful backup step.
