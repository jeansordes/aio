# Workflows Support Linear And Conditional Transitions

Workflows are declarative state machines.

They support:
- linear execution
- conditional transitions
- loops
- final states

Conditional transitions can use normalized runtime state for the latest step,
including provider status, exit code, whether files changed, and changed file
paths. They must not require parsing AI stdout.
