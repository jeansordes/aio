# Remove Backup On Successful Migration

When the new `.aio/` is complete and the migration is verified successful, the tool removes the backup directory. If migration fails or the user aborts, the backup remains and the new partial tree is handled in a way that does not leave the project without a recoverable state (document the recovery path in CLI output).
