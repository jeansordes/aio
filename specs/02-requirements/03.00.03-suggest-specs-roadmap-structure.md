# Suggest Specs Roadmap Structure

`aio init` can offer to scaffold project knowledge.

The suggested structure matches this repo:
- `specs/roadmap.csv`
- `specs/00-domains/`
- `specs/01-features/`
- `specs/02-requirements/`

This is optional.

It stays outside `.aio/`.
