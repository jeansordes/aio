# Create GitHub Release

Release flow must create GitHub release too.

npm release alone is not enough.

Create or reuse the GitHub Release before npm publish so a valid release tag
does not leave GitHub with only a tag when npm rejects the publish.
