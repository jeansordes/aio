# Provider Wrappers Are Replaceable

Providers are wrappers around external AI tools.

Each provider receives standard input from `aio` and returns normalized output.

Users can replace or add providers without changing workflow structure.
