# Provider Wrappers Are Replaceable

Providers are declarative entries in `.aio/providers.yaml`.

Each provider defines the command, arguments, prompt placement, and not-configured
message for an external AI CLI.

Users can replace or add providers without changing workflow structure.

`aio` captures provider stdout and stderr as logs only. It does not treat model
output as shared memory for later agents.
