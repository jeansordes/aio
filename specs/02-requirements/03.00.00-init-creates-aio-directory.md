# Init Creates Aio Directory

`aio init` creates `.aio/` when setup checks succeed, including a local provider
CLI check (see `03.01.03`). If that check fails, no `.aio/` is created.

Initial config includes:
- config
- providers
- roles
- workflows
- prompts
- schemas
