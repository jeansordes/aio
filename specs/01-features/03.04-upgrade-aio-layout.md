# Upgrade Aio Layout

User can refresh an existing `.aio/` tree to a newer `aio init` output without losing hand-edited work.

The flow uses a backup folder, a clean init to the current template, then a migration pass driven by the user’s choice of provider at upgrade time, and removes the backup on success.
