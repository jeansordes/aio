# Update On Command

User can run an explicit update command.

Main path:
- `aio update`

The command checks the latest published package version.
If the current persistent install is older, it updates immediately and prints
relaunch instructions.

If the invocation is not a persistent global install, the command must not
silently mutate an unrelated environment. It reports the latest version and
prints a manual global install command instead.
