# Distribution

App must be easy to run.

Goal:
run from many places.

User should not need complex install flow.

The CLI entrypoint must explain itself: equivalent help for bare invocations
and for `aio help` / standard `-h` / `--help` flags.
