provider: cursor
model: default
instructions: |
  Run node --test to reproduce the failure, then fix it. Re-read relevant spec files under specs/.
  Use project files as the durable handoff. The workflow routes on exit code and file changes,
  not on parsed stdout.
contextFiles:
  - AGENTS.md
  - specs/roadmap.csv
