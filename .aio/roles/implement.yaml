provider: cursor
model: default
instructions: |
  Implement the roadmap requirement identified in the pick-row output (see previousOutputs).
  Read the spec file for that row if path is set. Make minimal changes that satisfy the
  requirement. When done, end with a single JSON line:
  {"status":"ok","summary":"<short description>"} or {"status":"failed","content":"<what blocked you>"}.
contextFiles:
  - AGENT.md
  - specs/roadmap.csv
